#include <stdio.h>

int main(){
    int i = 5;
    printf("the value after i++ is %d\n", ++  i );
    // i++; pehle print pher increment
    // ==i; phele incrememnt phir print
    printf("the value of i %d\n", i);
    i+=10; // increments i by 10
    printf("the value of i %d\n", i);

    return 0;
}